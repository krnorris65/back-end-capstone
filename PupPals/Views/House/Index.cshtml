@model IEnumerable<PupPals.Models.House>

@{
    ViewData["Title"] = "House List";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Add New House</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Address</th>
            <th> City</th>
            <th>State</th>
            <th>Zip Code</th>
            <th>My House</th>
            <th>Avoid</th>
            <th> Notes</th>
            <th>Owners </th>
            <th> Pets </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZipCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsResidence)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Avoid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                @foreach (var owner in item.OwnerList)
                {

            @Html.DisplayFor(modelItem => owner.FirstName) <span></span> @Html.DisplayFor(modelI => owner.LastName) <br />

                }
                @*<a asp-controller="Pet" asp-action="Create">Add Pet</a>*@
            </td>
            <td>
                @foreach (var pet in item.PetList)
                {

                    @Html.DisplayFor(modelItem => pet.Type) <span>-</span> @Html.DisplayFor(modelI => pet.Name) <br />

                 }
                <a asp-controller="Pet" asp-action="Create">Add Pet</a>
            </td>

            </tr>

        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>

}
    </tbody>
</table>
